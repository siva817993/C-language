#include<stdio.h>
void main()
{
    int *ptr;
    printf("%u\n",ptr);
    int *ptr1=NULL;
    printf("the value in ptr1 is :\n %u",ptr1);
    if(ptr==NULL)
        printf("\nthe memory allocation is not done:");






}
